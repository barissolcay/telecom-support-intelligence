# Contributing

Create a focused branch, add tests for behavioral changes, and run `make test` before opening a pull request. Do not commit real customer data, credentials, model binaries, raw logs, or unreviewed generated datasets. New knowledge documents must declare product, language, version, effective date, and lifecycle status. Evaluation changes must preserve group-aware splitting and publish the resulting report artifacts.
